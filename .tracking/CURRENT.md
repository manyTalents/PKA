# PKA — CURRENT
> History: `PROGRESS.md` · Decisions: `DECISIONS.md` · per-item detail = dated PROGRESS entries.

## Active
- **Team IDENTITY upgrade** — all 30 rebuilt to gold-standard template; Onyx+Edge merged (29 active). 🔴 NOT committed, Owner reviewing diffs. `specs/2026-06-24-team-identity-upgrade-design.md`.
- **Watchtower Phase 0+0.5** — alt-data observatory ✅ **DEPLOYED + LIVE on droplet 104.131.176.130** (2026-06-26). Container `watchtower` Up (docker compose, restart unless-stopped); scheduler running with all 7 jobs; DB initialized at `/app/data/watchtower.db`; `.env` written (chmod 600) with ACTIVE bot Gmail app-pw (BW item `c863e928`, same as VEOE). **End-to-end SMTP alert VERIFIED** — `[WATCHTOWER] Deploy smoke test` sent + landed in inbox. Build: 8 tasks 31/31 + opus whole-branch review (3 blockers fixed) + Phase 0.5 all 4 scrapers live-verified (insider/EDGAR direct fetch, pred-markets Kalshi+Polymarket, philly-fed PDF, jobs Greenhouse/Lever ATS). `Documentos/watchtower/` head `aef0c1a`. Caveat: jobs watchlist sector labels loose (approx ticker map). **First cycle VERIFIED 06-27** (Up 35h healthy): pred-markets 241, job_postings 68, insider_filings 2 (thin — under-collecting, on fix list), philly-fed next Tue. ✅ **Phase-1 HARDENING DEPLOYED LIVE 2026-06-28** (HEAD `e78a2a7`, container rebuilt; 9 jobs now incl `liveness_digest_weekly` Sun 11:00 UTC + `db_backup_daily` 02:00 UTC): per-cycle run-summary logging + weekly liveness digest + SIGTERM graceful shutdown + insider yield fix/sector-tagging (**live yield 2→12 confirmed**) + SPY baseline at detection + nightly SQLite→gdrive backup. Kit: 47/47 tests. Backup VERIFIED end-to-end (`wt-2026-06-28.db` in `gdrive:Watchtower-Backups`); rclone baked into image + authorized `gdrive` remote mounted from droplet `/root/.config/rclone` (dir-mount so token persists). Caveat: sector-overlap only engages on clusters detected AFTER this deploy (existing live clusters have sector=NULL — not a regression). (DATA ✅) political-trading signal scoped — brief in Owner's Inbox `watchtower-political-trading-signal-2026-06-27.md`. **Owner GREEN-LIT House+Senate build** as a new `congress_trades` confluence domain: House Clerk bulk files (primary) + Senate eFD (1b), compute-our-own trailing-12mo alpha-vs-SPY "well-performing" rank (≥5 buys, top ~15-20), alerts = top-performer buy / 2+ member cluster-14d / ≥$250K, daily 06:30 UTC; honest caveat ~45-day disclosure lag → confirmer not standalone edge. **QUEUED behind Kit's hardening** (shares confluence/scheduler/schema/SPY files — build on top to avoid conflicts). (Glass, queued) Watchtower panel on /money hub.
- **AllTec / MTM Manager** — ✅ **v2.2.9/code24 SHIPPED to Play INTERNAL track** 2026-06-27 (build 99d255d4, submission 9cfda676; per-device download ~32-34MB). Carries LAB-01 hours-race fix + COL-01 invoice flt fix (backend already live). Full Maestro regression on v2.2.9 = **10/11 PASS**; LAB-01's remaining red is a TEST-HARNESS artifact only (flow's commit-edit tap doesn't blur the TextInput so labor-hours-display-button assert can't match) — the REAL data-loss fix is VERIFIED working (1.5 persists through pull-to-refresh on-device, screenshot lab01-05). ⚠️ **Coverage caveat:** this is a regression SUBSET on an EMULATOR, not full functional testing — RCP/QUE/CHK flows are shallow/semi-automated (no camera, no real offline), and card/pay-link payments, full job lifecycle, scheduling, photo upload, web/office parity (WEB-01 is a Playwright stub) are UNTESTED. Full realistic coverage = separate effort (use MOBILE-CATALOG.md/SYSTEM-REVIEW.md as checklist + a physical device). Earlier same-day: **v2.2.8 Maestro regression: 10 PASS / 1 FAIL after fixes** (ran against LIVE backend, Owner-approved RED-A, all ZZTEST data + financial docs cleaned up both rounds). ✅ **COL-01 real bug FIXED + DEPLOYED LIVE:** `finalize_invoice` (invoice.py) called `flt` unimported → NameError 500 on re-finalize path; added `from frappe.utils import flt` (main `8ea7d05`, verified, COL-01 now PASS). ✅ TAB-01 fixed (seed now has 5 active jobs incl ZZTEST-TAB-001/002; commit `05c3640`). 🔴 **LAB-01 STILL FAIL — 2nd REAL BUG (mobile, not yet fixed):** manual labor-hours save has a 500ms debounce that races pull-to-refresh → entered hours revert to server value; fix = fire saveHours immediately when isManual (LaborRateSection.tsx) + flow assert on display button. Needs a mobile rebuild/OTA to land (awaiting Owner go). Adam app login added to BW (`fd3526b4`). **Expanded EMULATOR coverage 2026-06-28:** Swift authored 15 new flows → COVERAGE-MATRIX.md (57 functions; 26 flows total; 12 deferred-need-testID; 4 not-emulator-feasible). Full 26-flow run = 15 PASS / 11 FAIL, but **all 11 fails are TEST-HARNESS issues (0 real product bugs)** — 5 flow syntax errors (crashed at parse), wrong fixtures (JOB-02 accept job was pre-assigned so accept button correctly hidden; SEND-01 job already advanced), timing/selectors (AUTH-01 greeting, MAT-01 "My Truck", HOME-01 below-fold, LAB-01 blur). **CONFIRMED WORKING on emulator:** accept-job, finish-job (timer+finish+send-to-check), schedule, inventory, receipts(Vision OCR live+clean), custom parts, payments(cash), tabs, notes. ⚠️ **OFFLINE still UNVERIFIED** (QUE-02/03 crashed at parse — re-run pending after harness fix). Pre-Monday readiness audit: no P0 code blocker; most prior bugs already fixed in v2.2.9. **Built+STAGED (not deployed):** invoice branding "AllTec Invoice" print format + constants "Lows"→"Lowe's"+"Cash" (Forge, branch `feat/invoice-branding-constants-monday` 3bd0b00; needs logo+review-URL from Owner); web office parity labor-desc+checklist edit (Glass, branch `feat/web-office-parity-monday` 46c905a, build green). Swift fixed all 11 harness issues (commit 0bc2374, incl. unassigned ACCEPT fixture + valid offline-flow syntax). 🔄 **Re-run of the 11 fixed flows IN PROGRESS** (first real offline test). Open: deploy the 2 staged fix groups (backend migrate+restart + web→Vercel) after re-run; Owner to provide logo + Google-review URL for invoice branding.
- **VEOE** — v5 re-cut (`veoe:v5-4e437aa`), 160 pass / 10 fail (pre-existing). Awaiting 10T cutover on Owner go; rollback `veoe:v4b-0730638`. Handled separately.
- **QuickBooks→ERPNext** — phased; All Boats Rise LLC pilot. 🔴 GATED on Erica delivering 2 QBO keys to BW. `specs/2026-06-15-accounting-quickbooks-replacement-design.md`.
- **MTP Prep App** — closed testing, 18 testers, pending Google review.

## Machine (crypto) — CLOSED + SHELVED 2026-06-20
19 strategies tested, 0 survive honest rigor; real premiums killed by execution cost, not fake edge. PAPER + idle. Say "Machine" only to revisit.

## Pending / infra / known issues
- Native Stripe; admin delivery + feedback survey; VEOE weekly backtest fix (BacktestRunner→BacktestMLFeed in main.py); Composio MCP wiring (key in BW); Owner PC audio reboot pending (Realtek ALC236).
- Minor: `cancel_all()` swallows cancel errors then clears internal levels list.
- Infra: ERPNext Docker Frappe on 134.199.198.83. GPKA = Grok lean workspace (`Documentos\GPKA`, live junctions to PKA).
